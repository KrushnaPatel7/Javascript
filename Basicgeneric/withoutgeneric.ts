// Two different functions for different types
function getNumberLength(num: number): number {
    return num.toString().length;
  }
  console.log(getNumberLength(20));
  function getStringLength(str: string): number {
    return str.length;
  }
  console.log(getStringLength("Goodmorning")); 

  